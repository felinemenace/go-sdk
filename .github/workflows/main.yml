name: CI

on:
  push:
    branches: [ master, dev ]
  pull_request:
    branches: [ master, dev ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: docker://golang:1
    - uses: actions/checkout@v1
    - name: go test
      run: go test -v ./...
